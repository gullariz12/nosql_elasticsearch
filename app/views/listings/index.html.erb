<p id="notice"><%= notice %></p>

<h1>Listings</h1>
<h3>Choose Filter</h3>
<%= form_tag(listings_path, method: :get) do %>
  <%= text_field_tag 'query[beds_min]', 'Min Beds' %>
  <%= text_field_tag 'query[beds_max]', 'Max Beds' %>
  <%= text_field_tag 'query[baths_min]', "Min Baths" %>
  <%= text_field_tag 'query[baths_max]', 'Max Baths' %>
  <%= submit_tag ('Search') %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Beds</th>
      <th>Baths</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @listings.each do |listing| %>
      <tr>
        <td><%= listing.beds %></td>
        <td><%= listing.baths %></td>
        <td><%= link_to 'Show', listing %></td>
        <td><%= link_to 'Edit', edit_listing_path(listing) %></td>
        <td><%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Listing', new_listing_path %>
